/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["sankavollerei.com", "i0.wp.com", "img.youtube.com"],
  },
};

module.exports = nextConfig;
